export interface Location {
    city: string;
    country: string;
}

export interface Weather {
    temp: number;
    min_temp: number;
    max_temp: number;
    feels_like: number;
    humidity: number;
    wind_speed: number;
}
